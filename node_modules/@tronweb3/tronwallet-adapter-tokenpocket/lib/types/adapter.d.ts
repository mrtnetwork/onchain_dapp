import { Adapter, AdapterState, WalletReadyState } from '@tronweb3/tronwallet-abstract-adapter';
import type { Transaction, SignedTransaction, AdapterName, BaseAdapterConfig, Network } from '@tronweb3/tronwallet-abstract-adapter';
export interface TokenPocketAdapterConfig extends BaseAdapterConfig {
    /**
     * Timeout in millisecond for checking if is in TokenPocket App.
     * Default is 2 * 1000ms
     */
    checkTimeout?: number;
    /**
     * Set if open TokenPocket app using DeepLink.
     * Default is true.
     */
    openAppWithDeeplink?: boolean;
}
export declare const TokenPocketAdapterName: AdapterName<"TokenPocket">;
export declare class TokenPocketAdapter extends Adapter {
    name: AdapterName<"TokenPocket">;
    url: string;
    icon: string;
    config: Required<TokenPocketAdapterConfig>;
    private _readyState;
    private _state;
    private _connecting;
    private _wallet;
    private _address;
    constructor(config?: TokenPocketAdapterConfig);
    get address(): string | null;
    get state(): AdapterState;
    get readyState(): WalletReadyState;
    get connecting(): boolean;
    /**
     * Get network information.
     * @returns {Network} Current network information.
     */
    network(): Promise<Network>;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    signTransaction(transaction: Transaction, privateKey?: string): Promise<SignedTransaction>;
    multiSign(...args: any[]): Promise<SignedTransaction>;
    signMessage(message: string, privateKey?: string): Promise<string>;
    private checkAndGetWallet;
    private checkIfOpenApp;
    private checkReadyInterval;
    private checkForWalletReady;
    private _checkPromise;
    /**
     * check if wallet exists by interval, the promise only resolve when wallet detected or timeout
     * @returns if wallet exists
     */
    private _checkWallet;
    private _updateWallet;
    private setAddress;
    private setState;
}
//# sourceMappingURL=adapter.d.ts.map