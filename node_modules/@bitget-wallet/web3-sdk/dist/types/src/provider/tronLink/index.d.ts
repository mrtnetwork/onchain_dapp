import type { ReqestAccountsResponse, TronWeb } from './types';
export type { TronWeb };
export interface TronLink {
    ready: boolean;
    tronWeb: TronWeb;
    request(config: Record<string, unknown>): Promise<ReqestAccountsResponse | null>;
}
export interface TronLinkProvider {
    tronLink: TronLink;
    tronWeb: TronWeb;
}
