import { AbstractAdapter } from './AbstractAdapter';
import type { TronLinkProvider } from '../provider';
type TronLinkAdapterOptions = {
    isBitkeep?: boolean;
    debug?: boolean;
};
declare class TronLinkAdapter extends AbstractAdapter {
    constructor(Options?: TronLinkAdapterOptions);
    getProvider(): TronLinkProvider;
}
export { TronLinkAdapter };
